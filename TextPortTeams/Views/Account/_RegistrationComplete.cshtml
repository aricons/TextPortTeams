@model TextPortCore.Models.RegistrationData

<input type="hidden" id="hidAccountId" value="@Model.AccountId" />

<div class="modal-dialog" role="document" id="purchaseModal">
    <div class="modal-content">
        <div class="modal-header">
            <h2 class="modal-title" id="addContactLabel">@Model.CompletionTitle</h2>
        </div>
        <div class="modal-body">
            <div class="form-group">
                <div id="lblConfirm">@Html.Raw(Model.CompletionMessage)</div>
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal" onclick="registration_complete('@Model.PurchaseType')">OK</button>
        </div>
    </div>
</div>